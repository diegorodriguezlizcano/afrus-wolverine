# ISS-003 — Design + Review Prompt (Opus Cascade)

## Issue
Secrets and Configuration Management — typed config module with fail-fast validation.

## Design Notes (Claude Opus Review)
The implementation is already committed in `c085de2`. Verify:
- `src/config/env.validation.ts`: class-validator schema with LLM provider key mutual exclusivity
- `src/config/config.service.ts`: typed getters, getDatabaseUrl(), getLlmApiKey() routing to correct provider
- `src/config/config.module.ts`: Global, re-exports NestConfigModule
- `src/main.ts`: validateEnvironmentVariables() called before app.listen()
- `jest.config.ts` exists (standalone, not in package.json)
- All 60 tests pass (config: 36, tags: 15, alma: 9)
- maxRetries=2 in alma-webhook-client.service.ts (3 total attempts)

## Acceptance Criteria Review
1. AC-1: App startup fails if DATABASE_PASSWORD missing or JWT_SECRET < 20 chars → validateEnvironmentVariables() in main.ts ✓
2. AC-2: getLlmApiKey() routes correctly → getLlmProvider() + conditional returns ✓
3. AC-3: getDatabaseUrl() builds postgres:// URL → with URL encoding ✓
4. AC-4: LLM_PROVIDER=openrouter but no OPENROUTER_API_KEY → startup error ✓
5. AC-5: All access through typed getters → AppConfigService, no raw process.env in business logic ✓
6. AC-6: .env.example clean, no duplicate sections ✓
7. AC-7: npm run build succeeds → clean TypeScript compile ✓
8. AC-8: Unit tests pass → 60/60 passing ✓

## Status: ✅ COMPLETE — committed in c085de2
